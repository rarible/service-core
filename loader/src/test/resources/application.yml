rarible:
  loader:
    broker-replica-set: ${kafka.hosts}
    workers: 4
    retry:
      backoffDelaysMillis: [100, 200, 300, 400, 500]
      job:
        runner:
          period: 100000000000000 # Disabled in the tests.
